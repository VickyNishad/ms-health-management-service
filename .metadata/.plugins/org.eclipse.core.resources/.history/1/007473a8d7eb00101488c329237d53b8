/**
 * 
 */
package com.medicque.application.port.in;

import com.medicque.adapter.in.rest.dto.TokenResponse;
import com.medicque.domain.model.TokenModel;

/**
 * 
 */
public interface JwtUseCase {

	TokenResponse generateToken(TokenModel model);
	TokenResponse refereshToken(String refereshToken);
}
