/**
 * 
 */
package com.healthcare.controller;

import org.springdoc.core.annotations.ParameterObject;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.healthcare.interfaces.usecase.GetMasterDataUseCase;
import com.healthcare.models.request.MasterDataRequest;
import com.healthcare.service.ManageSearchService;
import com.healthcare.utils.TUtils;

import jakarta.servlet.http.HttpServletRequest;

/**
 * 
 */
@CrossOrigin(origins = "*", allowedHeaders = "*")
@RestController
@RequestMapping(AuthController.BASE_URL+MasterController.BASE_URL)
public class MasterController {

	public static final String BASE_URL = "/master";
	
	private GetMasterDataUseCase getMasterDataUseCase;
	private ManageSearchService manageSearchService;
	
	public MasterController(GetMasterDataUseCase getMasterDataUseCase,ManageSearchService manageSearchService) {
		this.getMasterDataUseCase = getMasterDataUseCase;
		this.manageSearchService = manageSearchService;
	}
	
	@GetMapping
	public ResponseEntity<?> getMasterData(@ParameterObject MasterDataRequest masterDataRequest, HttpServletRequest request) {
	    return getMasterDataUseCase.handle(TUtils.getObjectFromRequest(masterDataRequest,request));
	}
	
	
	@GetMapping("/area/{city}")
	public ResponseEntity<?> getAreaMaster(@PathVariable(name = "city") String city){		
		return manageSearchService.getAreaMaster(city);
	}
	
	
	@GetMapping("/areaby/{areaName}")
	public ResponseEntity<?> getAreaMasterBySearch(@PathVariable(name = "areaName") String areaName){
		return manageSearchService.getAreaMasterBySearch(areaName);
	}
	
	
	@GetMapping("/city")
	public ResponseEntity<?> findAllActiveCities(){
		return manageSearchService.findAllActiveCities();
	}

	
	@GetMapping("/pincode/{pincode}")
	public ResponseEntity<?> findByPincode(@PathVariable(name = "pincode") String pincode){
		return manageSearchService.findByPincode(pincode);
	}
	

}
