/**
 * 
 */
package com.document.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import com.document.interfaces.usecase.DocumentUploadUseCase;
import com.document.models.GenericModel;
import com.document.models.request.DocumentRequest;

/**
 * 
 */
@RestController
@RequestMapping()
public class DocumentController {
	
	private DocumentUploadUseCase documentUploadUseCase;
	
	public DocumentController(DocumentUploadUseCase documentUploadUseCase) {
		this.documentUploadUseCase = documentUploadUseCase;
	}
	
	@GetMapping("/get")
	public String getString () {
		return  "";
	}


	@PostMapping(value = "/upload", consumes = "multipart/form-data")
	public ResponseEntity<?> uploadDocument(@ModelAttribute DocumentRequest documentRequest) {
//		MultipartFile file = documentRequest.getMultipartFile();
//
//		// Validate & process the file and metadata
//		System.out.println("File Name: " + documentRequest.getFileName());
//		System.out.println("Reference ID: " + documentRequest.getReferenceId());
//		System.out.println("Uploaded File: " + file.getOriginalFilename());
//
//		// ... Upload to S3, call DB, etc.
//
//		return ResponseEntity.ok("File uploaded successfully.");
		GenericModel<DocumentRequest> genericModel = new GenericModel<DocumentRequest>();
		genericModel.setPayload(documentRequest);
		
		return documentUploadUseCase.handle(genericModel);
	}

}
